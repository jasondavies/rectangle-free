# Archived GPU experiments

These translation units are exact research probes and diagnostic programs,
not production solver dependencies. They remain buildable through their
original Make targets so the corresponding entries in `experiments.md` can be
reproduced.

Active GPU solvers and their shared implementation remain at the repository
root. New production code must not include files from this directory.

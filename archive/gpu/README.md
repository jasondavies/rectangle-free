# Archived GPU experiments

These translation units are exact research probes and diagnostic programs,
not production solver dependencies. They remain buildable through their
original Make targets so the corresponding entries in `docs/experiments.md` can be
reproduced.

Active GPU solvers and their shared implementation live in `src/gpu/`. New
production code must not include files from this directory.
